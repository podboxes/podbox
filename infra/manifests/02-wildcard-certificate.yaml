apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: podbox-wildcard-tls
  namespace: default
spec:
  secretName: podbox-wildcard-tls
  issuerRef:
    name: letsencrypt-prod-dns
    kind: ClusterIssuer
  dnsNames:
  - "podbox.io"
  - "*.podbox.io"
